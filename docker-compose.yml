services:
  app:
    build: 
      context: .
    container_name: splitwise-backend
    volumes:
      - ./app:/app
    ports:
      - 8000:8000
    command: >
      sh -c "uvicorn main:app --host 0.0.0.0 --port 8000 --reload"